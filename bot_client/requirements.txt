# Bot Test Harness Additional Dependencies
# 
# The test harness uses the bot's main dependencies (see ../requirements.txt)
# This file only lists ADDITIONAL dependencies needed for the test harness.
#
# Installation:
#   pip install -r ../requirements.txt  # Install bot dependencies first
#   pip install --no-cache-dir -r requirements.txt     # Then install test harness extras

# Rich console output for pretty formatting
rich>=13.0.0
python-dotenv>=1.0.0
redis>=5.0.0

# MCP support (duplicated from parent for clarity)
fastmcp
openai>=1.0.0
litellm>=1.30.0
aiohttp>=3.9.0
